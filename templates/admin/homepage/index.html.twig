{% extends 'base.html.twig' %}

{% block body %}
	<main aria-label="contenu principal" id="main">
		<div class="container-fluid">
			<div class="row">
				<div class="col">
					<h2>Administration</h2>
					<p>
						<a href="{{ url('admin.member.index')}}" class="btn btn-secondary">Membres</a>
					</p>
					<p>
						<a href="#" class="btn btn-secondary">Demandes de correction</a>
					</p>
					<p>
						<a href="{{ url('admin.contact.index') }}" class="btn btn-secondary">Messages de contact</a>
					</p>
				</div>
			</div>
		</div>
	</main>
{% endblock %}
